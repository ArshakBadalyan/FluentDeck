# Serves Flutter build output under /web/ (matches --base-href /web/).

server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;

  location /web/ {
    try_files $uri $uri/ /web/index.html;
  }
}
